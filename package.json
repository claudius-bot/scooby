{
  "name": "scooby",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "sync-models": "node scripts/generate-models-data.mjs --fetch"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "prettier": "^3.5.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
